<h2><%= gettext "Edit upload" %></h2>

<%= render "form.html", changeset: @changeset,
                        action: upload_path(@conn, :update, @upload) %>

<%= link gettext("Back"), to: upload_path(@conn, :index) %>
