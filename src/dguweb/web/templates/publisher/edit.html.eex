<h1 class="heading-xlarge"><%= gettext "Edit %{publisher}", publisher: @publisher.title %></h1>

<%= render "form.html", changeset: @changeset,
                        action: publisher_path(@conn, :update, @publisher.name) %>

<%= link gettext("Back"), to: publisher_path(@conn, :index) %>
