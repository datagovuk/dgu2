<h1 class="heading-xlarge">Add a publisher</h1>

<%= render "form.html", changeset: @changeset,
                        action: publisher_path(@conn, :create) %>

<%= link "Back", to: publisher_path(@conn, :index) %>
