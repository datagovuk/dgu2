<h1 class="heading-xlarge"><%= gettext "Add an organisation" %></h1>

<%= render "form.html", changeset: @changeset,
                        action: publisher_path(@conn, :create) %>

<%= link gettext("Back"), to: publisher_path(@conn, :index) %>
